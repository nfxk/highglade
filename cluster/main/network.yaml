general:
  endpoint: 10.30.0.200
  addresses:
    - 10.30.0.211/24
  routes:
    network: 10.30.0.0/24 # The route's network (destination).
    gateway: 10.30.0.1 # The route's gateway (if empty, creates link scope route).
  mtu: 1500 # The interface's MTU.

dns:
  primary: 8.8.8.8
  secondary: 1.1.1.1
