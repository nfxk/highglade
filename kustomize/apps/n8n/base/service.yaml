# base/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: n8n-loadbalancer
  namespace: apps
spec:
  selector:
    app.kubernetes.io/name: n8n
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5678
  type: LoadBalancer
